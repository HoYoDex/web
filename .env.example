# Cap how many wiki pages are ingested. Unset means all ~2,000.
# Always set this while developing — a full ingest makes ~2,000 requests to a
# volunteer-run wiki, and you do not need them to adjust a margin.
HOYODEX_PAGE_LIMIT=50
