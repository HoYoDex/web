{
  "name": "hoyodex",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "deploy": "pnpm --filter web deploy"
  },
  "description": "The open, community-run index of every HoYoverse game."
}
